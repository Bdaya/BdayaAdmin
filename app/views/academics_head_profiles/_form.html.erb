<%= form_for(@academics_head_profile) do |f| %>
  <% if @academics_head_profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@academics_head_profile.errors.count, "error") %> prohibited this academics_head_profile from being saved:</h2>

      <ul>
      <% @academics_head_profile.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
